#include <iostream>
#define UNUSED(x) (void)(x)

int solve(int n, int m, std::vector<double> matrix, std::vector<double> x) {
    UNUSED(n);
    UNUSED(m);
    UNUSED(matrix);
    UNUSED(x);
    return 0;
}
